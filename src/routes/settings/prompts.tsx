// src/routes/settings/prompts.tsx — 占位，Task 4 整份覆盖
import { createFileRoute } from "@tanstack/react-router";

export const Route = createFileRoute("/settings/prompts")({
  component: () => null,
});
