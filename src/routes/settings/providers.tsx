// src/routes/settings/providers.tsx — 占位，Task 3 整份覆盖
import { createFileRoute } from "@tanstack/react-router";

export const Route = createFileRoute("/settings/providers")({
  component: () => null,
});
